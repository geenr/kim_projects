-- script that creates a table in current database called first_table
-- which has values of "id"(INT) and "name"(VARCHAR256)
CREATE TABLE IF NOT EXISTS `first_table` (`id` INT, `name` VARCHAR(256));
